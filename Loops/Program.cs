// do while loop
// condition is checked
// as long as the do condition is met, the while condition will be met
// the while loop will always be executed after checking the do condition is met

//using System.Net.NetworkInformation;

//Do -while loop - RepeatCharacter
//Using the do ...while loop, implement the RepeatCharacter method,
// which given a character and targetLength, should build a string of this character
// repeated the given number of times.

//The result must contain at least one character, so even if the targetLength is zero,
//the length of the result should be 1.
